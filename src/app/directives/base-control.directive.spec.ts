import { BaseControl } from './base.control';

describe('BaseControlDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseControl();
    expect(directive).toBeTruthy();
  });
});
